/* Script principal compartilhado (opcional) */
console.log("Ferramentas Online carregadas com sucesso!");

// Função para formatar números se necessário
function formatNumber(num) {
    return new Intl.NumberFormat('pt-BR').format(num);
}
